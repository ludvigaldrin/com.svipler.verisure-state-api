# com.svipler.verisure-state-api

To install:
Create a file config.js in root folder:
{
    "username" : "username",
    "password" : "password",
    "code": "pincode"
}

Then run:
npm install
node app.js
